# Maintainer: William Turner <willtur.will@gmail.com>

pkgname=xwayland
pkgver=1.20.1
pkgrel=1
pkgdesc="X server configured to (only) work with weston"
arch=('x86_64')
url="https://x.org"
license=('custom')
groups=()
depends=('libepoxy' 'xorg-proto' 'libxtrans' 'libxkbfile' 'wayland' 'eudev' 'libxfont' 'libbsd' 'nettle' 'libtirpc' 'pixman' 'xinit' 'libxkbcommon' 'libunwind' 'font-util' 'glproto')
makedepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
source=("https://dl.bintray.com/chromebrew/chromebrew/xwayland-1.20.1-chromeos-x86_64.tar.xz")
sha256sums=('b4bad45b9c4a9a7bd34d61c8b31e5a621133ed7cde17b85668c33b4632477f25')

package() {
  install -dm 0755 "$pkgdir/usr"
  cp -a usr/local "$pkgdir/usr"
}
